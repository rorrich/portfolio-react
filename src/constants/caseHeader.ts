/**
 * Верхняя «мёртвая зона» на страницах кейсов: пока скролл меньше этого значения,
 * не доверяем геометрии для переключения прозрачного/заполненного состояния шапки
 * (стабильнее у верхнего края страницы).
 */
export const CASE_HEADER_TOP_GUARD_PX = 80
